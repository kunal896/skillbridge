# Shared Contracts

This folder defines the data shapes that modules agree on.

Think of a contract like a plug/socket:
- one module produces the data
- another module consumes it
- both agree on the exact shape

Do not change a contract casually. Tell the team first.
